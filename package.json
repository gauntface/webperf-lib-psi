{
  "name": "pagespeed-monitor",
  "description": "A simple tool which crawls a sitemap and gets a PageSpeed Insight score for each page",
  "version": "0.0.1",
  "dependencies": {
    "express": "3.x",
    "gpagespeed": "^0.0.7",
    "xml-stream": "0.4.4"
  },
  "devDependencies": {
    
  }
}
